//describe es como si fuera el feature en selenium, es la funcionalidad, ejem: login
//it sirve para declara el test y passes es el nombre del test
describe('template spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
})